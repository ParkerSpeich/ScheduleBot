files:
    "/etc/cron.d/mycron":
        mode: "000644"
        owner: root
        group: root
        content: |
            * * * * * ec2-user /var/app/current/worktext.py
commands:
    dump_files:
        command: "find /var > /opt/python/log/find.log"